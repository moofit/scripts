This script will read the user's SMBHome attribute from AD and mount it.

You can use the mount_protocol variable at the top of the script to change between afp & smb.

The logfile can go wherever you like but make sure it is writable by the logging in users.
